# GameSpec 项目配置
# 由 stable-core profile 安装。本文件必须在没有可选 beta 协议、角色、
# 技能、模板、工作流或运行时适配器时仍然有效。

project:
  name: ""
  genre: ""
  platform: ""
  engine: "Unreal Engine 5"
  target_audience: ""

gamespec:
  version: "1.2.0"
  mode: "semi-auto"
  language: "zh-CN"

documents:
  draft_suffix: ".ai.md"
  final_suffix: ".md"
  variable_prefix: "VAR_"

workflows:
  enabled: []
